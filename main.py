from projectile_class import Projectile

# Change intial values for new projectile.
myprojectile = Projectile(initial_velocity=100, initial_height=0, initial_angle=45)

# The projectile lass will automatically call print rounded values for range, total time, max height and impact velocity.
# To see unrounded values adjust and un-comment.

#print("Unrounded Flight Time is: ", myprojectile.total_flight_time)
